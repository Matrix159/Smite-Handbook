<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".SmiteApplication"
        android:allowBackup="true"
        android:icon="@mipmap/norse_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/norse_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterializedSmite">
        <activity
            android:name="com.matrix.presentation.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MaterializedSmite"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- If you want to disable android.startup completely. -->
        <!-- Read here for more information why we need to do this:
        https://developer.android.com/jetpack/androidx/releases/work#2.6.0-alpha01 -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="remove"/>
    </application>
</manifest>