package com.matrix.data

import com.matrix.domain.models.GodInformation

object SmiteRepositoryData {
  val firstPatchList = listOf(
    GodInformation.build(),
    GodInformation.build()
  )
  val secondPatchList = listOf(
    GodInformation.build(),
    GodInformation.build(),
    GodInformation.build()
  )
}